/*
 NOTE: Steps to add problems and test them

1. Add the problem file in its category folder (or create a new folder if needed).
2. If you created a new folder, add `mod folder_name;` in `main.rs`.
3. Inside the folder, create (or update) `mod.rs` and declare the problem files as modules with `pub mod file_name;`.
4. Check `contains_duplicate.rs` for an example of how to write tests inside the file.
5. Run tests for a specific problem with: `cargo test <problem_file_name>`
    (e.g., `cargo test contains_duplicate`)
*/

mod arrays_n_hashing;

fn main() {
    println!("Hello, world!");
}
